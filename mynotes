Install virtual box  
Install centos 7 to start
Install default drivers
enable bidirectional for copy in settings

install basic updates

commands:
============
yum install epel-release.noarch
yum update -y
reboot
yum install telnet -y
yum install vim -y
yum install dkms gcc make kernel-devel bzip2 binutils patch libgomp glibc-headers glibc-devel kernel-headers -y


======
git bash first command
echo $'\n'"export PS1='${PS1//\\n/}'" >> ~/.bash_profile
